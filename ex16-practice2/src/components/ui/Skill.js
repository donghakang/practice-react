import React from 'react';

export default function Skill() {
    const prior_lang = ['python', 'javascript', 'C++', 'Java', 'Android', 'JSP', 'Processing'];
    const exposed_lang = ['PHP', 'Node js', 'Unreal Engine', 'Unity/C#', 'iOS'];

    const priorStyle = {color: 'black'};
    const exposedStyle = {color: 'gray'};

    return (
        <div>
            skills go here....
        </div>
    );
}
